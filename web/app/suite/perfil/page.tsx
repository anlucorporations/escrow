// TrueKeate — Suite: Mi Perfil (módulo en construcción — se completa en el Ciclo 8)
import { Card } from "@/components/Card";
export default function PaginaPerfil() {
  return (
    <Card className="p-6">
      <h1 className="font-display text-xl font-bold text-navy-800">Mi Perfil</h1>
      <p className="mt-2 text-sm text-navy-800/60">
        Este módulo se completa en la integración final (Ciclo 8). Acceso según rol y estado (RF-14).
      </p>
    </Card>
  );
}
