// =============================================================================
// TrueKeate — Tipos compartidos
// =============================================================================

/** Información de un contrato desplegado (dirección + ABI). */
export interface ContractInfo {
  direccion: string;
  abi: unknown[];
}
